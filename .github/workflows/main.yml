name: CI/CD
  
on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm run test

      - run: docker build -t frontapp .
      - run: docker run -p 8080:80 frontapp
      
  
